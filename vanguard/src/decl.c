typedef unsigned int Size;

typedef enum {
    FEATURE_GET_FOREGROUND_WINDOW = 0,
    FEATURE__END
} FeatureCode;

typedef enum {
    MESSAGE_GET_FOREGROUND_WINDOW = 0,
    MESSAGE__END
} MessageCode;